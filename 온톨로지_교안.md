# 온톨로지 학습 교안 (수연님 맞춤)

> **목표**: 온톨로지의 핵심을 손으로 익혀서, ① 서초 AI 페스타에 쓸 수 있는 지식그래프를 만들고 ② 부동산 온톨로지로 확장하며 ③ 온토에어 커뮤니티에 레퍼런스를 들고 합류한다.
>
> **실습실(lab)**: `tykimos/onto-osint-yuseong-event` 레포 — 매 모듈마다 "레포에서 볼 곳"을 짚는다.
>
> **러닝 스타일**: 개념 → 레포에서 실물 확인 → 내 도메인(서초 AI 페스타/부동산)으로 손으로 옮겨보기. **읽기만 하지 말고 매 모듈 실습을 꼭 완성할 것.**

---

## 한눈에 보는 전체 지도

```
M0 멘탈모델: 온톨로지란? (트리플)        ← "왜 폴더/태그가 아니라 온톨로지인가"
   ↓
M1 클래스 & 인스턴스 (세상을 '종류'로)    ← schema.json ↔ instances.json
   ↓
M2 속성 & 관계 (개체를 잇기)             ← operates / hostsAt / partOfSeries
   ↓
M3 추론 (온톨로지가 '똑똑한' 이유)        ← same_dong_combo (신뢰도 0.92)
   ↓
M4 지식그래프 (살리기 + 시각화 + 진화)    ← kg/YYYY-MM-DD.json, Mermaid, 온토에어
   ↓
M5 실전 (내 온톨로지 만들기)             ← onto-osint fork → 서초 AI 페스타
   ↓
부록 표준/도구 (RDF·OWL·SPARQL, 언제 필요한가)
```

**권장 페이스**: 1주 1모듈 (M0~M2는 가볍게 묶어도 됨). 페스타가 10/17이니 9월 전 M5 완성 목표.

---

## M0. 멘탈모델 — 온톨로지란 무엇인가

### 왜 배우나
폴더·태그·엑셀은 "내가 분류한 것"만 담는다. 온톨로지는 **"개체들이 서로 어떤 관계인지"**를 담아서, 사람이 적지 않은 사실까지 컴퓨터가 **추론**하게 만든다. 큐레이션·추천·자동 리포트가 여기서 나온다.

### 핵심 개념 — 딱 4개 + 1개
온톨로지는 세상을 이 4가지로 본다:

| 요소 | 영어 | 뜻 | 예 |
|---|---|---|---|
| 개체 | Instance / Entity | 세상에 실제 존재하는 '그것 하나' | 서초문화예술공원, 부스#12 |
| 클래스 | Class / Type | 개체들의 '종류' | 행사장, 체험부스 |
| 속성 | Property / Attribute | 개체가 가진 값 | 개최일=10/17, 부스수=50+ |
| 관계 | Relation | 개체와 개체를 잇는 선 | 참여기업A **가-운영한다→** 부스#12 |

**가장 중요한 단위 = 트리플(Triple)**: 모든 지식을 `주어 - 서술어 - 목적어`로 쪼갠다.
```
(서초AI페스타)   - (is a) -     (지역축제)          ← 클래스 소속
(참여기업A)      - (운영한다) -   (부스#12)           ← 관계
(서초AI페스타)   - (개최일) -    ("2026-10-17")      ← 속성
```
지식그래프 전체는 결국 **이 트리플의 거대한 묶음**이다. 이거 하나만 체화하면 나머지는 다 응용이다.

### 레포에서 볼 곳
- `reports/2026/05/2026-05-23.md` 의 Mermaid 다이어그램 → 노드(개체) + 화살표(관계)가 바로 트리플의 그림 버전.

### 실습 ✏️
서초 AI 페스타에 대해 **트리플 7개**를 손으로 적는다. 최소 1개는 관계(개체→개체), 1개는 속성(개체→값), 1개는 클래스 소속(is a).

### 완료 체크 ☐
- [ ] "트리플 = 주어/서술어/목적어"를 누가 물어도 설명할 수 있다
- [ ] 클래스와 인스턴스의 차이를 예로 말할 수 있다 (붕어빵 틀 vs 붕어빵)

---

## M1. 클래스 & 인스턴스 — 세상을 '종류'로 나누기

### 핵심 개념
- **클래스 = 종류(붕어빵 틀)**, **인스턴스 = 실제 개체(구운 붕어빵 하나)**.
- 클래스는 **계층(taxonomy)**을 이룬다: `행사장 ⊃ 실내행사장 ⊃ 전시홀`. 이 위계 관계가 **is-a / 상위클래스(subClassOf)**.
- 위계의 힘: "전시홀은 실내행사장이다"라고만 적어두면, "실내행사장의 성질"을 전시홀이 **자동 상속**한다. (← 추론의 씨앗)

### onto-osint 대응
- `ontology/schema.json` = **클래스/속성 정의** (틀)
- `ontology/instances.json` = **실제 장소·기관·동 누적 DB** (붕어빵들)
- 핵심 클래스 위계: `Event → Venue → Dong`, 그리고 `Organization, Activity, AgeGroup`

### 서초 AI 페스타로 옮기면
```
클래스 트리(초안)
- 행사(Event)
   └ 부스세션 / 강연 / 체험프로그램 / 공모전
- 공간(Venue)
   └ 전시존 / 피지컬AI존 / 무대
- 주체(Organization)
   └ 참여기업 / 기관 / 공동기획단
- 기술분야(AITopic)
   └ 생성형AI / 로봇(피지컬AI) / 음성 / 비전 ...
- 대상세대(AgeGroup)
   └ 아동 / 여성 / 장애인 / 어르신 / 가족전연령
```

### 실습 ✏️
위 클래스 트리를 **수연님 버전으로 다시 그린다.** 부스 3개를 골라 각각 어느 클래스의 인스턴스인지, 속성 2개씩(주최, 대상세대 등) 붙인다.

### 완료 체크 ☐
- [ ] `schema.json`과 `instances.json`이 각각 무엇을 담는지 안다
- [ ] 내 도메인 클래스 트리 1장 완성

---

## M2. 속성 & 관계 — 개체를 잇기

### 핵심 개념 — 두 종류의 '서술어'를 구분하라
1. **속성(Datatype property)**: 개체 → **값(리터럴)**. `개최일 = "2026-10-17"`, `팔로워수 = 30000`
2. **관계(Object property)**: 개체 → **다른 개체**. `참여기업A -운영→ 부스#12`, `부스#12 -위치→ 피지컬AI존`

관계엔 **방향**과 **도메인/레인지**(어떤 클래스→어떤 클래스만 연결 가능)가 있다.
예: `hostsAt` 의 도메인=Event, 레인지=Venue → "이벤트는 장소에서 열린다"만 허용.

### onto-osint 대응 (리포트 Mermaid의 화살표 라벨들)
- `operates` (기관 → 이벤트): 누가 운영하나
- `hostsAt` (이벤트 → 장소): 어디서 열리나
- `partOfSeries` (이벤트 → 시리즈): 반복 프로그램의 한 회차
- `crossDongCombo` (이벤트 ↔ 이벤트): 같은 날 다른 동 연계

→ **관계 이름을 잘 짓는 게 온톨로지 설계의 절반이다.** 동사처럼 짓고 방향을 명확히.

### 서초 AI 페스타로 옮기면
```
참여기업A  -operates→  부스#12
부스#12   -locatedIn→  피지컬AI존
부스#12   -demos→      로봇(AITopic)
강연#3    -targets→    어르신(AgeGroup)
공동기획단  -promotes→   부스#12        ← 홍보/마케팅과의 연결고리
```

### 실습 ✏️
M1에서 만든 부스 3개를 **관계 5개로 잇는다.** 각 관계에 도메인/레인지를 적는다(`promotes: Influencer → Event`처럼). 그림(노드+화살표)으로도 그려본다.

### 완료 체크 ☐
- [ ] 속성 vs 관계를 즉시 구분한다
- [ ] 내 도메인 관계 5개 + 방향 정의 완성

---

## M3. 추론 — 온톨로지가 '똑똑한' 이유

### 핵심 개념 — 적지 않은 사실을 컴퓨터가 만들어낸다
- **명시적 사실**: 내가 적은 트리플.
- **추론된 사실**: 규칙에 따라 컴퓨터가 **새로** 도출한 트리플.
- 추론의 종류:
  - **상속**: "전시홀 ⊂ 실내행사장" + "실내행사장은 우천대체 가능" → "전시홀은 우천대체 가능" (자동)
  - **규칙(if-then)**: "같은 동 + 같은 날 두 이벤트면 → 콤보로 묶어라"
  - **전이/대칭**: A-인접-B, B-인접-C ⟹ A-인접-C (전이성)

### onto-osint 대응 (이게 이 레포의 알맹이)
`CLAUDE.md` + 리포트의 "추론 결과"에서:
| 규칙 | 의미 | 신뢰도 |
|---|---|---|
| `same_dong_combo` | 같은 동·같은 날 → 동선 콤보로 번들 | 0.92 |
| `same_date_cross_dong` | 같은 날 다른 동 → 연계 추천 | 0.80 |
| `operator_kid_friendliness` | 도서관·과학관 운영 → 어린이 친화 가산점 | — |
| `indoor_rainy_fallback` | 우천 시 같은 동 실내 대체 후보 | — |

**관찰 포인트**: 추론마다 **신뢰도 점수(0~1)**가 붙는다. 온톨로지가 100% 확신이 아니라 "얼마나 그럴듯한가"를 다룬다는 것. 추천·큐레이션의 정렬 기준이 여기서 나온다.

### 서초 AI 페스타로 옮기면 (직접 번역해보기)
- `operator_kid_friendliness` → `physical_ai_zone_bonus` : 피지컬AI존 부스 가산점
- `same_dong_combo` → `same_zone_combo` : 같은 존 부스 동선 추천
- `indoor_rainy_fallback` → `crowded_alt_booth` : 인기 부스 대기 길면 같은 주제 대체 부스 추천
- (홍보) `promo_priority` : 화제성 높은(피지컬AI·공모전 등) 부스 → 기획단 홍보 우선순위↑

### 실습 ✏️
서초용 추론 규칙 **2개**를 `if (조건) then (도출 트리플) [신뢰도]` 형식으로 직접 쓴다. 그중 1개는 기획단 홍보 업무와 연결되게.

### 완료 체크 ☐
- [ ] "명시적 사실 vs 추론된 사실"을 구분한다
- [ ] 추론 규칙을 if-then으로 직접 쓸 수 있다
- [ ] 신뢰도 점수가 왜 필요한지 설명할 수 있다

---

## M4. 지식그래프 — 살리기 + 시각화 + 진화

### 핵심 개념
- **지식그래프(KG) = 온톨로지(틀) + 인스턴스(실제 데이터)** 를 합쳐 실제로 돌아가게 한 것.
- **스키마 자동 진화**: 새 엔티티(처음 보는 기업/기술)가 들어오면 스키마가 닫혀있지 않고 **열린 채로 자라난다**(open-world). onto-osint가 매일 이걸 한다.
- **스냅샷**: 하루치 그래프를 `ontology/kg/YYYY-MM-DD.json`로 저장 → **시간에 따른 변화 추적**이 가능 (예: `evt-027: D-1 → D-day`).

### onto-osint 대응
- `ontology/kg/2026-05-23.json` : 그날의 지식그래프 스냅샷
- 리포트의 Mermaid : 그 스냅샷의 **사람용 시각화**
- 변경 추적 섹션 : 어제 대비 상태 전이

### 온토에어 연결점 ★
온토에어 = **온톨로지 시각화** 도구. 즉 M4의 "Mermaid 시각화"를 훨씬 강력하게 만든 것이 온토에어다.
→ 서초 페스타 KG를 온토에어로 **인터랙티브하게 띄우면 = 10/17 전시 부스 콘텐츠**가 된다. (수연님이 온토에어 합류 때 들고 갈 첫 레퍼런스)

### 실습 ✏️
`kg/2026-05-23.json` 하나를 열어 **노드 5개 + 관계 5개를 손으로 골라** Mermaid로 직접 그려본다(`graph TD`). KG(JSON) → 그림으로 옮기는 감각을 만든다.

### 완료 체크 ☐
- [ ] KG = 온톨로지 + 인스턴스라는 걸 안다
- [ ] kg.json 스냅샷을 읽고 Mermaid로 옮길 수 있다
- [ ] 온토에어가 이 그림에서 무슨 역할인지 설명할 수 있다

---

## M5. 실전 — 내 온톨로지 만들기 (onto-osint fork)

### 목표
앞 모듈에서 손으로 적은 것들을 **실제 돌아가는 시스템**으로 만든다. "딱 2파일만 바꾸면 도메인이 바뀐다"는 설계를 이용한다.

### 순서
1. 레포 fork/clone → `onto-osint-seocho-aifesta`
2. `config/osint-config.json` 수정: 지역(서초구), 출처(서초구청·페스타 공식·AI행사 매체), 클래스 시드(M1 결과), 타겟존(전시존/피지컬AI존…)
3. `CLAUDE.md` 수정: 추론 규칙(M3 결과) + 도메인 판단 기준
4. 하루치 파이프라인 실행 → 첫 리포트 1장 생성
5. 결과를 보고 규칙·클래스 다듬기 (반복)

### 부동산으로 확장 (장기)
같은 엔진을 한 번 더 fork:
- `Event→Venue→Dong` ⟹ `매물→단지→생활권`
- 추론: `역세권_가산점`, `동일생활권_연계`, `가격조정_임박(D-3)`
- → 온토에어 부동산 온톨로지 = 태영님이 기대한 "수연님이 어느정도 할 수 있는 상태"

### 완료 체크 ☐
- [ ] 서초용 config/CLAUDE.md 작성
- [ ] 첫 일일 리포트 1장 생성
- [ ] 부동산 확장 매핑표 초안 작성

---

## 부록. 더 깊이 — 표준과 도구 (지금 당장은 몰라도 됨)

onto-osint는 JSON 기반의 실용 온톨로지다. 학계·산업 표준으로 더 깊이 가려면:

| 키워드 | 한 줄 정의 | 언제 필요? |
|---|---|---|
| **RDF** | 트리플의 표준 표기법 | 데이터 교환·공개 시 |
| **RDFS / OWL** | 클래스·관계·추론을 형식적으로 정의하는 언어 | 엄밀한 추론·검증 필요 시 |
| **SPARQL** | 지식그래프용 질의 언어(SQL의 그래프판) | 그래프에서 복잡한 검색 시 |
| **Protégé** | 온톨로지 설계 GUI 툴 | OWL 본격 설계 시 |
| **Neo4j** | 그래프 DB | 대규모 KG 저장·질의 시 |

**순서 추천**: M0~M5(실용)로 감각을 먼저 만들고 → 필요할 때 RDF/OWL로 "형식화" → SPARQL로 "질의". 표준부터 들어가면 대부분 중도 포기한다.

### 더 읽을거리 (가볍게)
- "What is an Ontology" — 트리플/클래스/추론 개념 검색
- 지식그래프(Knowledge Graph) 입문 자료
- onto-osint 본가 레포 README (본가 메커니즘 이해)

---

## 진도표 (체크하며 진행)

| 모듈 | 핵심 산출물 | 상태 |
|---|---|---|
| M0 | 트리플 7개 | ☐ |
| M1 | 서초 클래스 트리 | ☐ |
| M2 | 관계 5개 + 방향 | ☐ |
| M3 | 추론 규칙 2개(if-then) | ☐ |
| M4 | kg.json → Mermaid 1장 | ☐ |
| M5 | 서초 config/CLAUDE.md + 첫 리포트 | ☐ |
